# Motion AI

The project bootstrap command is:

```bash
npx motion-ai
```

Run it interactively when adding or updating the Motion MCP integration, approve only the MCP servers and skills needed by the team, and keep the resulting configuration out of production runtime images. Motion is used in the application through the `motion` package and the shared animation primitives in `src/components`.
